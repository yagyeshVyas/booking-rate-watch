{
  "name": "booking-rate-watch",
  "version": "2.0.0",
  "private": true,
  "description": "Free Booking.com mobile-rate watcher + local management dashboard: own property vs competitors, emailed every 5h via GitHub Actions.",
  "scripts": {
    "watch": "node ratewatch.js --config config.json",
    "dashboard": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.16",
    "playwright": "^1.60.0"
  }
}
