{
  "name": "booking-rate-watch",
  "version": "1.0.0",
  "private": true,
  "description": "Free Booking.com mobile-rate watcher: own property vs competitors, emailed every 5h via GitHub Actions.",
  "scripts": {
    "watch": "node ratewatch.js --config config.json"
  },
  "dependencies": {
    "nodemailer": "^6.9.16",
    "playwright": "^1.60.0"
  }
}
